using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class oncolenter : MonoBehaviour
{
	public GameObject offer;
    // Start is called before the first frame update
    void Start()
    {
        offer.active = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    
}
