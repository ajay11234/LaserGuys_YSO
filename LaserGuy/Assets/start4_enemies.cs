using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class start4_enemies : MonoBehaviour
{
    // Start is called before the first frame update
    public spawn_enemies spawn;
    void Start()
    {
        spawn.startspawn2();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
